// MapTiler API key
// Set this to your own MapTiler API key to enable the styled base map.
// Get a free key at https://cloud.maptiler.com
// Without a key, the map falls back to OpenFreeMap (Liberty style).
window.MAPTILER_KEY = '';
